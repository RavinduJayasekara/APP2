import {ACCSUMMARY} from '../../data/dummy-data';

const initialState = {
    accSum: ACCSUMMARY,
}

const accSumReducer = (state = initialState,action) => {
    return state;
}

export default accSumReducer;